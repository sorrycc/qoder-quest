# Password for the login page. Falls back to 12306 when unset.
ADMIN_PASSWORD=12306

# Where phones reach this server, for the take-home QR codes. Only needed behind a proxy or tunnel that
# doesn't pass the public host along; otherwise the address in the browser is used.
# PUBLIC_URL=https://quest.example.com
